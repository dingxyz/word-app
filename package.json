{
  "name": "root-package",
  "version": "1.0.0",
  "scripts": {
    "start:all": "npm-run-all --parallel start:backend start:frontend",
    "start:backend": "npm --prefix ./back-end run start",
    "start:frontend": "npm --prefix ./front-end run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
